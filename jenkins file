pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/abrarulhaqhaq04-stack/hospital-management-system.git'
            }
        }

        stage('Build') {
            steps {
                echo 'Building the project...'
            }
        }
 stage('Test') {
            steps {
                echo 'Running tests...'
            }
        }
        stage('Deploy') {
    steps {
        bat 'scp -i "C:\\Users\\abrar ul haq\\Downloads\\workwithAWS.pem" -o StrictHostKeyChecking=no -r *.html css ubuntu@16.16.205.18:/tmp/hospital-deploy'

        bat 'ssh -i "C:\\Users\\abrar ul haq\\Downloads\\workwithAWS.pem" -o StrictHostKeyChecking=no ubuntu@16.16.205.18 "sudo rm -rf /var/lib/tomcat10/webapps/hospital && sudo mv /tmp/hospital-deploy /var/lib/tomcat10/webapps/hospital"'
    }
}    
    post {
        always {
            echo 'Pipeline completed.'
        }
        success {
            echo 'Build Succeeded!'
        }
        failure {
            echo 'Build Failed!'
        }
    }
}
